# Roadmap

> 🌐 语言 / Language: [中文](ROADMAP.md) | [English](ROADMAP_EN.md)

这份文件只做一件事：**把「修 bug」和「加功能」分到两个不同的版本里**，并且让这个分工在动手之前就写下来。

## 契约（先读这段）

**规则一 —— `fix` 钉在当前小版本，`feat` 进下一个大版本。**

| 类型 | 归属版本 | 例子 |
| --- | --- | --- |
| `fix`（把坏掉的东西修好，不改变已有行为的定义） | 当前小版本的下一发（如 `1.0.x`） | 手势失效、动画方向错、返回后显示错的东西 |
| `feat`（新行为、新交互、用户能感知到"多了个东西"） | 下一个大版本（如 `1.1.0`） | 触感反馈、新的占位/loading 策略、行为语义的重新统一 |
| `ui`（纯外观调整） | 看它是否改变行为：只换皮归 `1.0.x`，改了交互归 `1.1.0` | 按钮去掉文字 = 只换皮 → 小版本 |

判定用一句问话就够：**「用户会不会发现多了/少了一个能做的事？」** 会 → `feat`；只是「原本该这样、之前坏了」→ `fix`。

**规则二 —— 收到 `feat` 只登记，不在当轮实施。**

任何一轮会话里，如果用户提了一个 `feat`（或一个 `ui` 经由上面判据归到 `feat`），**当轮只把它写进下面 `1.1.0` 那个桶，不写实现代码**。
实施要等到对应的分支**真的签出**（例如 `git checkout -b v1.1.0-beta`）之后才开始 —— 那时才打开这个桶，把条目逐条搬进工作流。

> 这条规则是用户明确要求的：他自认有"把 feat 混进小版本"的倾向，所以要让**登记**与**实施**在时间上分离开，
> 而不是靠某轮的自觉。写下就是契约，绕过它就是违约。

**规则三 —— 桶里的条目在下一次触碰它时才有权移动。**

条目只能在三种情况下离开 `1.1.0` 桶：① 对应分支已签出并开始实施；② 用户明确说它改判成 `fix` 了；
③ 用户明确说不要了。**不允许**因为"顺手就改了"而静默实施 —— 顺手改的 `feat` 必须补登记，或者回滚。

## 1.0.x —— 只收 `fix`

到 `1.0.4` 这一发为止要修掉的东西。

> 全部为 `fix`：都属于「本来的行为坏了 / 和说好的不一样」，不引入新交互。

| # | 内容 | 状态 |
| --- | --- | --- |
| bug-1 | 侧栏边缘滑动在**非 100% 缩放**下失效；且移动端长按唤起「选画廊」滑块后**瞬间就释放**了 | 已修（1.0.4） |
| bug-2 | 平板**横屏 + 左手**：`PreviewCard` 不贴边；Android 返回手势触发的是僵硬的「退后一页」动画，而不是自然的收合 | 已修（1.0.4） |
| bug-3 | 平板上连续点开 2 个以上画廊后，用 `PreviewCard` 顶部返回按钮：当前卡片消失后**又露出了上一个画廊的** `PreviewCard` | 已修（1.0.4） |
| bug-4 | 改完元数据后，`PreviewCard` 应**就地刷新**（当前是旧数据留在卡片上） | 已修（1.0.4） |
| bug-5 | 在**尾页**长按应当**禁用兔子洞**（当前仍会跳走） | 已修（1.0.4） |
| bug-7 | 从**兔子洞建议**进入的阅读**不计入历史** | 已修（1.0.4） |
| bug-8 | 搜索后打开一个画廊，返回时**搜索结果没保留**（回到空 feed） | 已修（1.0.4） |

## 1.1.0 —— 只收 `feat`

**这些在签出 `1.1.0-beta` 之前一律不实施。** 本轮（`1.0.4`）只做了上面的 `1.0.x` 桶。

| # | 内容 | 为什么不进小版本 |
| --- | --- | --- |
| feat-6 | 「猜你想看」的行为要和兔子洞**统一**：点击去详情页而不是直接开车；或者从阅读器返回时回到**当前画廊自己的** `PreviewCard` | 重新定义了一处交互语义，用户能明显感到行为变了 → `feat` |
| feat-9 | 缩略图轮盘加**触感反馈**（haptic feedback） | 纯新增的用户可感知能力 → `feat` |
| ui-10 | 缩略图轮盘**快速滚动时显示占位图** | 新增了一个新状态（占位）与随之而来的加载策略 → `feat`，不是换皮 |
| ui-11 | 「刷新」可能是多余的，可以考虑**移除** | 删掉一个用户能做的事，属于功能面收缩 → `feat` |
| ui-12 | 移动端筛选按钮**去掉文字**、与 PC 对齐以省空间 | 只换皮、不改行为 → 虽然像 `ui`，但用户已明确把它归到 `1.1.0` |

## 与其他文件的关系

- **本文件是唯一的版本分配清单。** 别在 `AGENTS.md`、`README` 或某轮聊天里再抄一份 —— 抄出来的那份必然会漂移。
- `AGENTS.md`（仓库根，gitignored）里只有**规则本身**（"收到 feat 只登记不实施"），它会指向本文件；**条目内容不重复**。
- 一个版本真正开工时：签出对应分支 → 从对应桶里搬条目 → 完成后按 `CHANGELOG.md` 的惯例记录。
- 版本号机制（`scripts/bump_version.py`、`1.0.x` 的 `fix` 语义）见 [CONTRIBUTING.md](CONTRIBUTING.md)；
  发布工程守卫见 [STARTUP.md](STARTUP.md) 第 7 节。
